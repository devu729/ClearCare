# ── ClearCare Frontend — copy to .env and fill in values ──
# NEVER commit .env to Git

# Your Supabase project URL (visible on dashboard)
VITE_SUPABASE_URL=https://kvxfldvpuguvndtcrxzh.supabase.co

# Supabase anon/public key (Project Settings → API → anon public)
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Backend URL (local dev)
VITE_BACKEND_URL=http://localhost:8000

# Clinician org codes — comma separated, no spaces
VITE_CLINICIAN_ORG_CODES=DEMO2024
